#include <stdio.h>
#include "main.h"

/**
 * print_most_numbers - prints the numbers except 2 and 4
 *
 * Return: Always 0.
 */

void print_most_numbers(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		if ((n == 50) || (n == 52))
		{
			continue;
		}
		_putchar(a);
	}
	_putchar(10);
}
